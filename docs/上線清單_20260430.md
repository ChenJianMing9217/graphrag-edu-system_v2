# 上線清單 — 2026-04-30

> 每完成一項打勾 `[x]`，未做維持 `[ ]`。  
> 預估時間皆為單項耗時，總時程依賴未做事項。

---

## P0 上線前必做（不做則無法上線）

### 1. 4 個 Bug 修復驗證（重啟 app.py 跑下列 query）

- [x] **#1.1 top_domain 修復**  
   query：`我是問目前小朋友認知功能有問題嗎`  
   驗收：console log 出現 `Fetching Subdomain: '認知功能'`（不是「口語理解」）

- [x] **#1.2 SQL bug 修復**  
   query：`台北市信義區哪裡可以進行早期療育`  
   驗收：console log 出現 `[MySQLClient] 找到機構 N 筆`（N > 0）

- [x] **#1.3 Web Search 路由**  
   query：同上  
   驗收：console log 出現 `[Engine] GPT Web Search 啟動`

- [x] **#1.4 本地 Qwen 摘要**  
   query：同上  
   驗收：console log 出現 `[Engine] 本地摘要：XXX → YYY 字`

- [x] **#1.5 chat.completions 路由（避免亂呼叫 web_search）**  
   query：`PR15 是什麼意思`  
   驗收：console log 出現 `[Engine] GPT Fetch 成功（通用知識）`，**不**走 web_search

- [x] **#1.6 Layer 1 多任務識別**  
   query：`目前小朋友的狀況應該去哪個機構治療`  
   驗收：DST log 顯示 `多任務偵測：主任務=H，副任務=['D' or 'C']`

---

### 2. 治療師帳號 + 真實匿名報告匯入

- [ ] **#2.1** 確認需要幾位治療師（5-10 位）
- [ ] **#2.2** 匿名化現有兒童評估報告（PDF → 結構化）
- [ ] **#2.3** 為每位治療師產生 access code
- [ ] **#2.4** 將 child_id ↔ access_code 對應寫入 DB
- [ ] **#2.5** 隨機抽 1 個 access code 自己跑一輪確認綁定正確

---

### 3. CI/CD push 到新 GitHub repo

- [ ] **#3.1** 在 v7 root 跑 `git init`
- [ ] **#3.2** `git add .` 後檢查沒誤加 `.env` / `*.db`
- [ ] **#3.3** 第一次 commit 並 push 到 `https://github.com/ChenJianMing9217/graphrag-edu-system_v2`
- [ ] **#3.4** GitHub Actions 跑 `ci.yml` 成功（image 推到 ghcr.io）
- [ ] **#3.5** 確認 ghcr.io 上能看到 image：`ghcr.io/chenjianming9217/graphrag-edu-system_v2:latest`

---

### 4. Self-hosted Runner（Ubuntu）

- [ ] **#4.1** 在 v6 同一台 Ubuntu，新增 runner instance（資料夾 `~/actions-runner-v7/`）
- [ ] **#4.2** 註冊到 v2 repo（GitHub → Settings → Actions → Runners）
- [ ] **#4.3** runner 狀態顯示 Online
- [ ] **#4.4** 確認 deploy.yml 自動觸發成功（push 後幾分鐘）

---

### 5. 部署環境

- [ ] **#5.1** Ubuntu 上 `git clone` v2 repo（或 self-hosted runner 自動 checkout）
- [ ] **#5.2** 在專案目錄建 `.env`（`OPENAI_API_KEY` / `HF_TOKEN` / `MYSQL_HOST` / `USE_WEB_SEARCH=1` / `SUMMARIZE_GPT=1`）
- [ ] **#5.3** `docker compose -p edu_sys_v7 -f docker-compose.yml up -d`
- [ ] **#5.4** `docker logs edu_sys_v7-app-1` 確認沒 crash、模型載入完成
- [ ] **#5.5** 內網瀏覽器連 `http://Ubuntu_IP:5002`，登入頁面打得開
- [ ] **#5.6** 外網（或 VPN 環境）連得到 → 治療師會用的方式
- [ ] **#5.7** 跑一個冷啟動 query，確認 LLM 回得了

---

### 6. Streaming Generation 上線

- [ ] **#6.1** 後端 `/api/chat` 改 SSE：`stream=True` + `Response(stream_with_context(...))`
- [ ] **#6.2** 前端 `index.html` 改用 `EventSource` 或 `fetch + ReadableStream`
- [ ] **#6.3** 收到 `event: done` 時才 render feedback button（保留 message_id）
- [ ] **#6.4** 確認 ChatMessage 寫入 DB 完整性（streaming 中每 token 不要分別寫 DB，只在結束時寫）
- [ ] **#6.5** 本地測試打字機效果（首字 0.5-1s 內出現）

---

## P1 上線前強烈建議

- [ ] **#P1.1 冷啟動測試**：app.py 第一次啟動 + 第一個 query 不超時
- [ ] **#P1.2 連續 30+ turn 壓力測試**：自己跑，確認 Neo4j / Memory 不漏
- [ ] **#P1.3 故障注入測試**：故意拔 OpenAI key、斷 Neo4j，確認不直接 5xx
- [ ] **#P1.4 備份 MySQL**：`mysqldump early_intervention_db_v2 > backup_20260430.sql`
- [ ] **#P1.5 清掉 E2E 測試 ChatMessage**（避免 monitor 統計被污染）

---

## 上線後通知

- [ ] **#7.1** 發送治療師存取碼 + [使用說明](docs/治療師測試使用說明.md) 連結
- [ ] **#7.2** 提供 LINE / email 緊急聯絡管道
- [ ] **#7.3** 提醒：每個回答記得按 👍/👎

---

## P2 治療師測試期間做（不影響上線）

- [ ] **#P2.1** Held-out 35 個標註（90-120 分鐘）→ 跑 [held_out_review.py](rl_pipeline/scripts/held_out_review.py)
- [ ] **#P2.2** 跑 [eval_held_out.py](rl_pipeline/scripts/eval_held_out.py) 出 markdown 報告
- [ ] **#P2.3** 每天執行 [monitor_live_data.py](rl_pipeline/scripts/monitor_live_data.py) 觀察治療師資料
- [ ] **#P2.4** 修兩個 cosmetic warning（HAS_FORMAL_ASSESSMENTS / Clinical Enrichment fulltext lexical error）
- [ ] **#P2.5** 累積足夠 (state, action, feedback) → 跑 Layer 2 BC pretrain

---

## 重要檔案位置

| 用途 | 路徑 |
|------|------|
| Web Search + 摘要 | [retrieval_module_v2/execution_engine.py](retrieval_module_v2/execution_engine.py) |
| top_domain 修復 | [dialogue_manager.py:309-318](dialogue_manager.py#L309-L318) |
| SQL bug 修復 | [retrieval_module_v2/execution_engine.py:239-256](retrieval_module_v2/execution_engine.py#L239-L256) |
| Layer 1 prototypes | [dialogue_state_module/config/task_scope_prototypes.jsonl](dialogue_state_module/config/task_scope_prototypes.jsonl) |
| TASK_SOFT 門檻 | [dialogue_state_module/semantic_flow_module_v2.py:577](dialogue_state_module/semantic_flow_module_v2.py#L577) |
| Held-out 35 個 | [rl_pipeline/scripts/held_out/raw_held_out_combined.json](rl_pipeline/scripts/held_out/raw_held_out_combined.json) |
| 治療師說明 | [docs/治療師測試使用說明.md](docs/治療師測試使用說明.md) |

---

## env 變數總表（.env 範例）

```bash
# OpenAI
OPENAI_API_KEY=sk-xxx
USE_WEB_SEARCH=1               # 1=啟用 web_search_preview, 0=關閉

# 摘要
SUMMARIZE_GPT=1                # 1=啟用本地摘要, 0=關閉
SUMMARIZE_THRESHOLD=500        # 超過 N 字才摘要
SUMMARIZE_MAX_TOKENS=300

# HuggingFace
HF_TOKEN=hf_xxx

# MySQL（如果跟 Ubuntu 不同網段，改用對應 IP）
MYSQL_HOST=192.168.150.136
MYSQL_DATABASE=early_intervention_db_v2
```
